# rohit.onl

This repository contains the Eleventy source for `https://www.rohit.onl`.

## Internal Authoring Handbook

The publishing and editing guides live in `docs/authoring/`. Start with `docs/authoring/index.md` if you want the overview, then jump to the task-specific guides for posts, pages, media, navigation, and FAQ.

When a code change affects publishing flow, navigation, feed behavior, layouts, front matter, content locations, or authoring/build commands, update the handbook in the same change.

## Local Commands

```bash
pnpm dev
pnpm build
```
